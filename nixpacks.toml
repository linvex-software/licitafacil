[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install -g pnpm@9.15.0", "pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["pnpm --filter @licitafacil/api build"]

[start]
cmd = "node apps/api/dist/main.js"
